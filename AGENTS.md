# AGENTS.md

**Version 1.0 — 2026年8月24日**  
**対象リポジトリ：`my-library`**

## 1. リポジトリの目的

`my-library` は、個人用の知識・学習資料・プロンプト・説明書・画像を管理し、GitHub および GitHub Pages から閲覧するためのライブラリです。

主な閲覧環境は Android スマートフォンです。次を優先してください。

1. 既存資料とユーザー意図の保護
2. GitHub Pages での正常表示
3. スマートフォンでの視認性と操作性
4. Markdown・HTML・画像・JSON・索引の整合性
5. 小さく、安全で、追跡可能な変更
6. 将来の自動処理と検索のしやすさ

ローカルでは OneDrive 配下に置かれる場合がありますが、ローカル絶対パスはリポジトリへ記録しないでください。

---

## 2. 指示の優先順位

適用順は次のとおりです。

1. 現在のタスクでユーザーが明示した最新の指示
2. 対象ファイルに最も近い階層の `AGENTS.override.md`
3. 対象ファイルに最も近い階層の `AGENTS.md`
4. このルート `AGENTS.md`
5. 既存のリポジトリ構造・命名・デザイン・自動化
6. Codex の一般的な判断

下位階層の指示は、その配下のファイルに対して、このルート指示を具体化または上書きできます。

指示が曖昧または競合している場合は、本文・ID・公開URL・関連付けを壊さない選択を優先し、PR の Notes に判断を記載してください。

---

## 3. Codex の基本動作

Codex は、このリポジトリの整理・保守・更新担当として、説明だけで終わらず、依頼された成果物まで完成させてください。

作業時は次を守ってください。

- 作業前に、対象ファイルと直接関係するファイルを確認する
- 既存の仕組みがある場合は、新しい独自方式より既存方式を優先する
- 必要以上の変更を行わない
- 無関係な整形、言い換え、改行変更、キー並べ替えを避ける
- 既存本文と既存デザインを、明示的な依頼なしに変更しない
- 仮ファイル、未完成コード、ダミーリンク、架空データを残さない
- 実行していない確認を「確認済み」と報告しない
- リポジトリ外のファイルや `.git` ディレクトリを変更しない
- 認証情報、秘密情報、個人用絶対パスをコミットしない

---

## 4. Git・ブランチ・Pull Request

### 4.1 完了境界

通常の完了境界は次のとおりです。

**ブランチ作成 → ファイル変更 → 検証 → コミット → Pull Request 作成**

Codex は Pull Request まで作成して構いませんが、**Pull Request を merge してはいけません**。最終承認と merge はユーザーが行います。

### 4.2 ブランチ

- `main`、`master`、その他の既定ブランチへ直接コミットしない
- Codex Cloud が自動作成したブランチは、そのまま使用してよい
- 手動命名する場合は `codex/<type>-<short-kebab-case>` を基本とする
- 例：
  - `codex/docs-add-bookkeeping-guide`
  - `codex/fix-broken-image-links`
  - `codex/chore-update-metadata`
- 既存の作業ブランチや公開済み履歴を、無断で rebase・force-push しない
- リモートブランチを無断で削除しない

### 4.3 コミット

1つの論理的変更を、できるだけ1つのコミット単位にまとめてください。

推奨例：

```text
docs: 簿記3級教材を追加
fix: 画像とHTMLのリンク切れを修正
chore: meta.jsonと索引を更新
refactor: 小規模な資料フォルダ整理
```

大量の無関係な変更を1つのコミットへ混ぜないでください。

### 4.4 Pull Request

PR のタイトルと本文は、原則として日本語で簡潔に記載してください。

PR 本文には次を含めてください。

```markdown
## Summary
- 実施した内容

## Changed files
- 主要な変更ファイル

## Validation
- 実行した検証と結果
- 実行できなかった検証

## Notes
- 判断、互換性、残課題、ユーザー確認事項
```

表示に関わる変更では、利用可能ならローカルプレビューまたはブラウザ確認を行ってください。スクリーンショットを取得していない場合は、取得したふりをしないでください。

---

## 5. 変更規模と自動実施範囲

### 5.1 小規模変更として自動実施してよいもの

次をすべて満たす場合は、小規模変更として確認なしで実施して構いません。

- 移動・リネーム・整理の対象が原則5ファイル以内
- 1つの資料フォルダ、または隣接する同一カテゴリ内で完結する
- 既存 `id` を変更しない
- 公開URLや主要導線を壊さない
- JSONスキーマやカテゴリ体系を変更しない
- すべての参照先を同時に更新できる
- 本文内容と既存デザインを変更しない
- 検証可能で、元に戻せる

小規模変更を実施した場合も、PR の Notes に移動・リネーム内容を記載してください。

### 5.2 大規模変更

次は、ユーザーがその変更を明示していない限り実施せず、PR または報告で提案に留めてください。

- 6ファイル以上の移動・リネーム
- 複数カテゴリをまたぐ再編
- 公開URLの変更
- 既存 `id` の変更
- JSONスキーマの変更
- カテゴリ体系の変更
- 索引方式やGitHub Actionsの全面変更
- HTMLデザインシステムの全面統一
- 多数の本文・プロンプトの一括書き換え

### 5.3 削除

ユーザーが明示しない限り、既存の資料本体、HTML、JSON、画像、設定ファイルを削除しないでください。

今回の作業中に Codex 自身が生成した不要な一時ファイルは削除して構いません。重複や不要に見える既存ファイルは、勝手に削除せず PR の Notes へ記載してください。

---

## 6. 資料の役割分担

原則として、各形式の役割を次のように扱ってください。

- **Markdown = Knowledge / 本文・原本・Single Source of Truth**
- **HTML = Presentation / 説明書・閲覧版・GitHub Pages UI**
- **Image = Recognition / 一目確認・プレビュー**
- **`meta.json` = Structure / 識別・分類・検索・関連付け**
- **`index.html` またはその生成元 = Navigation / ライブラリ入口**
- **`library.json` = Index data / 複数 `meta.json` の集約結果**

本文の事実については、原則として Markdown を最優先してください。

1つの論理的な資料につき、原則1つの `meta.json` を置いてください。複数の無関係な資料を1つの巨大な `meta.json` にまとめないでください。

---

## 7. 資料追加・更新時の自動追従

新しい資料を追加した場合、または既存資料を移動・リネーム・実質更新した場合は、依頼文に毎回書かれていなくても、次を自動的に確認・更新してください。

1. 資料本体と関連ファイルのまとまりを判定
2. 同じ資料の `meta.json` を作成または更新
3. ライブラリの入口・索引へ掲載
4. 必要な場合だけ `library.config.json` 等の既存設定を更新
5. 相対リンク、画像参照、関連資料参照を修正
6. JSON・HTML・リンクを検証

### 7.1 索引の自動更新

新規資料は、既存ルールで非表示・下書きと明示されていない限り、索引掲載対象として扱ってください。

- `index.html` が手書き管理なら、既存構造とデザインに合わせて更新する
- `index.html` や `library.json` が GitHub Actions 等から生成される場合は、生成元の `meta.json`、設定、マニフェストを更新し、生成物を手作業で改変しない
- ファイル冒頭のコメント、README、workflow、生成スクリプトから「生成物」であることが分かる場合は、その仕組みを優先する
- 生成方式が不明な場合は、既存差分と参照関係を調べてから変更する

### 7.2 `library.json`

`meta.json` の追加・更新だけを依頼された場合、`library.json` を新規作成しないでください。

既存 `library.json` が自動生成物なら直接編集しません。ユーザーが明示的に集約更新を依頼した場合、または既存リポジトリの正式運用が手動更新であると確認できた場合のみ更新してください。

---

## 8. `meta.json` 標準仕様

### 8.1 動作モード

入力状況から、次を自動判定してください。

- **CREATE**：既存 `meta.json` がない資料から新規作成
- **UPDATE**：既存 `meta.json` と更新資料を比較し、必要な項目だけ更新
- **AUDIT**：構文、内容、参照、重複、欠落、資料との不一致を確認して修正
- **BATCH**：複数資料を判定し、1資料につき1つの `meta.json` を処理

### 8.2 情報源の優先順位

1. 今回のユーザーの明示指示
2. 既存 `meta.json` の安定フィールド
3. 原本 Markdown
4. 説明書・閲覧版 HTML
5. プレビュー画像
6. 実在するファイル名・フォルダ名
7. 安全に生成できる分類・要約

内容が矛盾する場合は、勝手に混ぜて整合したふりをしないでください。本文の事実は原本 Markdown を優先し、重要な矛盾は PR の Notes へ記載してください。

### 8.3 標準スキーマ

資料ごとの `meta.json` は、原則として次の基本構造を使用してください。

```json
{
  "schema_version": "1.0",
  "id": "",
  "title": "",
  "type": "",
  "category": "",
  "subcategory": null,
  "summary": "",
  "tags": [],
  "status": null,
  "version": null,
  "language": "ja",
  "dates": {
    "created": null,
    "updated": null
  },
  "files": {
    "source": null,
    "guide": null,
    "preview": null,
    "additional": []
  },
  "search": {
    "keywords": []
  },
  "relations": {
    "parent_id": null,
    "related_ids": []
  }
}
```

既存ライブラリに、これより具体的な正式スキーマや検証スクリプトがある場合は、既存の正式仕様を優先してください。

### 8.4 フィールド規則

- `schema_version`
  - 標準は `"1.0"`
- `id`
  - 資料を一意に識別する安定ID
  - 新規作成時のみ生成する
  - ユーザー指定ID、既存正式ID、意味のある短い kebab-case の順で優先する
  - 日本語のみで無理な英訳・ローマ字化が不安定になる場合は、短く一貫したUnicode IDでもよい
  - タイトル変更だけで再生成しない
- `title`
  - 原本 Markdown のH1または正式タイトルを優先する
- `type`
  - 原則として次から最も近いものを1つ選ぶ
  - `prompt`, `guide`, `study-note`, `report`, `plan`, `reference`, `template`, `project`, `note`, `other`
- `category`
  - 主所属は原則1つ
  - 初期候補：`prompts`, `study`, `work`, `philosophy`, `travel`, `reference`, `project`, `personal`, `other`
  - 既存ライブラリに正式カテゴリ体系がある場合は、そちらを優先する
- `subcategory`
  - 必要な場合のみ。不要なら `null`
- `summary`
  - 資料の目的・用途を中心に1〜2文
  - 日本語80〜180文字程度を目安とし、本文を長く複製しない
- `tags`
  - 人が横断分類・絞り込みに使う語を3〜8個
  - 重複語、ほぼ同義語、細かすぎるタグの乱立を避ける
- `status`
  - 資料から明確な場合のみ `draft`, `active`, `archived` 等を設定
  - 不明なら `null`
- `version`
  - 資料本文または既存情報で確認できる場合だけ設定
  - Codex が勝手に番号を作らない
- `language`
  - 日本語は `ja`、英語は `en`、主要言語を決められない複数言語は `multi`
- `dates.created`, `dates.updated`
  - 確認できる場合のみ `YYYY-MM-DD`
  - 現在日を作成日として勝手に代入しない
- `files.source`
  - 唯一の原本、または最も原本に近い実在ファイル
- `files.guide`
  - 説明書・閲覧版HTML。存在確認できた場合のみ
- `files.preview`
  - 1枚物の PNG / WebP / JPG 等。存在確認できた場合のみ
- `files.additional`
  - その他の実在する補助ファイル
- `search.keywords`
  - 検索用の語を5〜12個
  - 略称、表記ゆれ、専門語、用途を必要に応じて含める
- `relations.parent_id`
  - 親資料が明確な場合のみ。不明なら `null`
- `relations.related_ids`
  - 明確に関連する既存IDのみ。不明なら `[]`

### 8.5 安定フィールド

既存 `meta.json` がある場合、次は不用意に変更しないでください。

- `id`
- `dates.created`
- `relations.parent_id`
- `relations.related_ids`

既存値に明確な誤りがある場合だけ、根拠を確認して修正し、PR の Notes へ記載してください。

### 8.6 自動生成・更新してよい項目

資料内容から安全に生成・改善してよい項目：

- `title`
- `type`
- `category`
- `subcategory`
- `summary`
- `tags`
- `language`
- `search.keywords`
- `files`

ただし、既存値が妥当で資料と矛盾しない場合は、表現を変えるだけの無意味な更新を行わないでください。

### 8.7 推測禁止

根拠がない場合は、単一値を `null`、配列を `[]` としてください。

特に次を捏造しないでください。

- Version
- 作成日・更新日
- 著者・組織
- ライセンス
- 外部URL・出典
- 関連資料ID
- 実在しないファイル名

### 8.8 JSON品質

- 標準JSONのみ
- UTF-8
- 2スペースインデント
- コメント禁止
- 末尾カンマ禁止
- キー重複禁止
- `NaN` / `Infinity` 禁止
- 文字列は二重引用符
- 不明値を空文字 `""` で表さない
- 本文全文、プロンプト全文、長大な説明文を複製しない
- ファイル参照は資料フォルダ基準の相対パスを優先する
- Windows絶対パス、ローカルユーザー名、秘密情報を記録しない
- 保存前にJSONパーサーで検証する

---

## 9. HTML・GitHub Pages

### 9.1 既存デザインを維持

既存HTMLを変更するときは、現状デザインを維持してください。

明示的な再設計依頼がない限り、次を行わないでください。

- 配色の全面変更
- タイポグラフィの全面変更
- カード・ナビゲーション構造の全面変更
- 共通CSSへの一括移行
- 全ページの見た目の統一を目的とした大規模修正
- 本文の要約、言い換え、削除

必要な修正は、パス、リンク、レスポンシブ、アクセシビリティ、軽微な構造エラーなどに限定し、最小差分で行ってください。

新規HTMLは、同じカテゴリまたは最も近い既存ページのデザインを基準にしてください。

### 9.2 スマートフォン優先

HTMLには原則として次を含めてください。

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

次を確認してください。

- 主要本文がスマートフォンで無理なく読める
- 画像が画面幅を超えない
- 表やコードがページ全体を押し広げない
- 必要な箇所だけ横スクロールできる
- タップ領域が小さすぎない
- 見出し階層が自然
- 文字と背景のコントラストが不足しない
- 不要な外部依存を増やさない

最低限、画像には次の考え方を適用してください。

```css
img {
  max-width: 100%;
  height: auto;
}
```

### 9.3 GitHub Pages互換

次は禁止します。

```text
C:\Users\...
file:///C:/...
/Users/<name>/...
```

HTML、CSS、JavaScript、JSON、Markdownの参照には、原則として相対パスを使用してください。

ファイル名の大文字・小文字を区別して検証してください。Windowsで開けてもGitHub Pagesで失敗する参照を残さないでください。

### 9.4 完成済みHTMLの移植

完成済みHTMLを「GitHub Pages向け」「My Library向け」に変更する場合は、本文と既存デザインを維持し、次を中心に修正してください。

- ローカル絶対パス
- 画像・CSS・JavaScriptの参照
- GitHub Pagesで動かないリンク
- viewport
- 横幅・オーバーフロー
- ライブラリ用メタデータ
- 索引との接続

### 9.5 画像のみを資料化する場合

画像だけが資料として与えられた場合は、画像を見やすく閲覧できるHTMLラッパーを作成して構いません。

- 原則として完成HTMLは1ファイルを優先する
- 1枚または少数の画像で、HTMLが過度に大きくならない場合は Data URI 埋め込みを使用してよい
- 画像容量が大きい、枚数が多い、他ページでも再利用する場合は、画像を相対パスで参照する
- Data URI化と外部画像の二重保持を、理由なく行わない
- 画像の縦横比を壊さない
- スマートフォンで拡大・縮小しやすい表示にする

---

## 10. Markdown・プロンプト

Markdownは原本として扱い、明示的な編集依頼なしに本文を変更しないでください。

特にプロンプトファイルでは、次を勝手に変更しないでください。

- MUST / MUST NOT
- 必須 / 禁止
- 指示の優先順位
- 条件分岐
- 数値条件
- 出力形式
- ツール利用条件
- ファイル形式
- Version規則

プロンプトの改善を明示的に依頼された場合は、元の意図を保持し、矛盾、重複、曖昧さを最小差分で修正してください。

資料本文に Version がある場合、`meta.json.version` はその表記から取得してください。本文に独自のVersion更新規則がある場合は、その規則を優先します。単なるリンク修正、メタデータ修正、索引更新だけで本文Versionを増やさないでください。

---

## 11. パス・命名・フォルダ

- 既存の命名規則を優先する
- 日本語ファイル名は、問題なく使われている限り維持してよい
- 同名ファイルが異なるフォルダに存在しても、それだけを理由に変更しない
- ファイルはフルパス上の位置で識別する
- 好みだけでファイル名やフォルダ名を変更しない
- リネーム時は、HTML、Markdown、JSON、設定、索引の全参照を同時に更新する
- URL互換性が必要な既存パスは維持する
- 大文字・小文字だけのリネームは、Windows上でのGit挙動に注意する
- 文字コードと改行コードは、無関係な差分を避けるため既存ファイルに合わせる

---

## 12. OneDrive・Windows環境

ローカル作業場所が OneDrive 配下でも、リポジトリ内のファイルへそのパスを保存しないでください。

次を守ってください。

- `.git` を移動・同期解除・編集しない
- リポジトリ外へファイルを移動しない
- OneDriveの競合コピーを正規ファイルと断定しない
- `desktop.ini`, `Thumbs.db`, 一時ファイル、バックアップファイルを新規コミットしない
- OneDrive同期状態を確認できない場合、確認済みと報告しない
- 大量の移動・リネームを避ける
- ローカル環境だけで動く `file://` 参照を作らない

---

## 13. 検証

変更後は、利用可能な範囲で次を実施してください。

### 13.1 共通

- `git diff` で意図しない変更がないことを確認
- 既存の検証スクリプト、lint、testがあれば優先して実行
- 検証のためだけに大きな依存関係を追加しない
- 失敗した検証は隠さず、原因と未解決範囲を記載
- 実行できなかった検証は「未実行」と明記

### 13.2 JSON

- 変更した全JSONをパース
- 必須キー、型、重複キー、末尾カンマを確認
- `meta.json` のID重複を可能な範囲で確認
- `files.*` と `relations.*` の参照先を確認
- 既存安定フィールドの意図しない変更を確認

### 13.3 HTML・リンク

- 変更したHTMLに重大な構文崩れがない
- 相対リンクと画像参照の存在確認
- 大文字・小文字の一致
- ローカル絶対パスと `file://` が残っていない
- `index.html` または生成元から新規資料へ到達できる
- スマートフォン幅で横崩れがないことを、利用可能ならブラウザで確認
- 外部リンクを変更した場合は、利用可能なら到達確認

### 13.4 自動生成

- 生成スクリプトまたはGitHub Actions相当のローカル手順がある場合は実行
- 生成物を手編集していないことを確認
- 生成前後で不必要な大量差分が出た場合は、その差分をコミットせず原因を調査

---

## 14. Code Review Rules

Codex がPull Requestをレビューする場合は、次を重点的に確認してください。

### 資料整合性

- 新規・更新資料に対応する `meta.json` と索引更新が欠けていないか
- `meta.json` の安定ID、作成日、関連IDが不用意に変わっていないか
- `files.*` が存在しないファイルを参照していないか
- 同一資料や同一IDが重複登録されていないか

### GitHub Pages

- Windows絶対パス、`file://`、大文字・小文字不一致が残っていないか
- HTML、画像、CSS、JavaScriptへのリンク切れがないか
- スマートフォンで重大な横崩れや読めない縮小が発生しないか
- 既存デザインや本文が依頼範囲外で変更されていないか

### 変更安全性

- 生成物を直接編集していないか
- 大規模な移動・削除・リネームが、明示的な依頼なしに行われていないか
- 公開URL、資料ID、カテゴリ体系を不必要に壊していないか
- 認証情報、個人用絶対パス、秘密情報が追加されていないか

機械的な整形だけの指摘より、資料消失、リンク切れ、誤分類、公開不能、本文改変など、利用者への影響が大きい問題を優先してください。

---

## 15. 作業報告

作業完了時は、次を簡潔に報告してください。

```markdown
## Changed
- 実施内容

## Files
- 主要な変更ファイル

## Checks
- 実行した検証と結果
- 未実行の検証

## Git
- branch:
- commit:
- pull request:

## Notes
- 注意事項がある場合のみ
```

ブロックされた場合は、抽象的に「できません」とだけ書かず、次を明記してください。

- 何が不足しているか
- どこまで完了したか
- 変更は保存・コミットされたか
- PRを作成できなかった具体的理由

---

## 16. 完了条件

タスクは、原則として次をすべて満たしたときに完了です。

- ユーザーが依頼した成果物が実際に作成・更新されている
- 既存本文と既存デザインが、依頼範囲外で変更されていない
- 必要な `meta.json` が作成・更新されている
- 必要な索引またはその生成元が更新されている
- 相対リンクとファイル参照が整合している
- 実施可能な検証が完了している
- 変更が専用ブランチへコミットされている
- Pull Request が作成されている
- Pull Request は merge されていない

最重要原則は、**「勝手に全面改修する」より、「既存資産を安全に保ち、小さく完成させる」こと**です。
