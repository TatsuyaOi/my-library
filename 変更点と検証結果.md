# My Library Inbox化：変更点と検証結果

作成日：2026-09-05  
確認対象：TatsuyaOi/my-library / main  
確認時コミット：`6bef2fcefd88f5d702cde75bc1d32975cffbea5a`

## 現在の完了範囲

**更新ファイル16点、差異検査付き導入スクリプト、導入・運用説明を作成しました。**
ローカルの隔離テストは実施済みです。一方、このチャットのGitHub接続によるブランチ作成は
`403 Resource not accessible by integration` で拒否されました。
**リモートリポジトリの変更、ブランチ・コミット・PR作成、マージ、Pages公開は行っていません。**

実リポジトリからルート構成、AGENTS.md、一覧生成スクリプト、カテゴリテンプレート、workflowなどを読みました。
更新対象の既存ファイルは確認時のGit blob SHAに照合しています。
全資料の実体を一括取得したものではなく、既存全資料を使ったビルドは未実施です。
適用先で必ず既存全資料のビルド・公開対象検査を実行してください。

## 主な修正理由

### 1. Inboxの動作を具体化

`★仮置き保管庫` をリポジトリ直下の入口として設定します。
名前を英語inboxへ変更する必要はありません。
内容判定・分類はCodex、入力SHA照合・保存・検証はPythonが担当します。
入力を置いただけでCodexが自動起動する常駐処理は含みません。

### 2. 既存の索引方式を維持し、新規資料だけ拡張

実際の旧ビルダーは、基本情報をHTMLの `library-*` メタから読み、`meta.json` は主に画像関連付けに使っていました。
その従来動作は維持し、新Inbox資料にだけ管理用識別情報を追加します。
新資料では元HTMLを書き換えず `meta.json` から一覧情報を補完し、補助HTMLの二重掲載を避けます。
カテゴリ構成、既存資料の公開URL、本文、画像を一括再編しません。
古い添付の `library-all.json` で最新データを上書きしません。

### 3. 手動・自動の説明の混在を解消

旧 `使い方.txt` は `LIBRARY_ITEMS` への手動追記、旧 `導入手順.md` はActionsによる自動生成を案内していました。
旧資料には現在の設定と異なる `12_簿記` などのフォルダ例もありました。
説明を現行カテゴリとInbox方式に合わせて統一しました。
旧AGENTS.mdの詳細ルールは `docs/library-maintenance-rules.md` に全文保存し、新ルートAGENTSから参照します。

### 4. Pagesへの公開処理を明示

旧workflowは索引を自動コミットするところで終了していました。
GitHub公式仕様ではGITHUB_TOKENによるpushはPagesのビルドを起動しないため、
更新workflowは同じ実行で索引生成とPages artifactの公開を行います。
PR時は検証のみ、mainマージ後に公開します。
初回に利用者側で Settings → Pages → Build and deployment → Source を GitHub Actions に設定する必要があります。
現在の設定は変更していません。

### 5. 公開前原本と公開サイトを分離

確認時のリポジトリはPublicです。
新規Inbox原本は既定でGit追跡対象外にし、Pages用 `_site` へもコピーしません。
ただし既にGit追跡された原本には `.gitignore` は効きません。
Publicリポジトリへpushした原本は、Pagesに載せなくてもGitHub上で公開されます。
機密情報の自動検出・ウイルス検査・HTMLスクリプトの無害化を実装したものではありません。

## 変更対象16ファイル

| ファイル | 役割 |
|---|---|
| `AGENTS.md` | CodexのInbox手順、原本保持、公開確認、PRまでの完了条件 |
| `docs/library-maintenance-rules.md` | 元のAGENTS.mdを全文保存した詳細ルール |
| `docs/inbox-workflow.md` | 日常操作、計画JSON、Cloudとローカルの違い、制限 |
| `inbox.config.json` | Inbox名、サイズ上限、公開確認必須の設定 |
| `★仮置き保管庫/README.md` | 入口フォルダの使い方と注意 |
| `.gitignore` | Inbox原本、作業用計画、公開用一時出力などを除外 |
| `scripts/inbox.py` | scan、dry-run、原本を残した新規コピー、閲覧HTML/meta生成 |
| `scripts/inbox_index.py` | 新Inbox資料だけのメタデータ連携 |
| `scripts/build_library.py` | 従来ビルダーへの最小限の連携追加 |
| `scripts/prepare_pages.py` | 公開対象の選別、参照検証、Inboxを含めない `_site` 作成 |
| `tests/test_inbox.py` | 取り込みと互換性の回帰テスト35件 |
| `.github/workflows/rebuild-library.yml` | PR検証とmainからの索引生成・公開 |
| `index.html` | Inbox運用の説明、最近更新の順序修正、パス処理の修正 |
| `assets/category-index.html` | 同じ表示を維持したパス処理・入力タイミングの修正 |
| `使い方.txt` | 現行運用に更新 |
| `導入手順.md` | 初回設定、PR、Pagesの説明に更新 |

`library.config.json`、既存資料本体、`library-all.json`、各カテゴリの生成済みJSON/indexは更新セットへ含めていません。
各カテゴリのJSON/indexは、適用先で正式なビルダーを実行して再生成します。

## 実行した検証

| 検証 | 結果 | 条件・範囲 |
|---|---|---|
| Pythonユニットテスト | 35件成功 | 隔離した一時ディレクトリと合成資料 |
| 導入スクリプト検査 | 5項目成功 | 確認のみ、main上の拒否、16ファイル適用、再適用、差異時の全体停止 |
| Python構文検査 | 成功 | scripts / tests のcompileall |
| workflow YAMLの読取り | 成功 | ローカルパーサーによる構造確認。Actions上の実行ではない |
| Chromiumの画面描画 | 成功 | オフライン・合成データ・390pxと1440px |
| 一覧検索・最近更新順序 | 成功 | 合成データによるブラウザ内検証 |
| 日本語・空白・#を含むパス | 成功 | URL構築と生成先参照を検査 |
| 不正なURLの拒否 | 成功 | javascript: のリンクを無効化することを検査 |
| ページ全体の横はみ出し・JS例外 | 検出なし | 上記の合成データでの画面描画 |

35件では、原本一致、画像・テキスト閲覧ページ、旧HTMLメタ方式、入れ子index、.htm、
重複ID、再実行、上書き拒否、パス逸脱、シンボリックリンク、公開未確認、SHA不一致、
依存ファイル欠落、バッチ全体事前確認、Inboxの公開出力除外などを検査しました。
実行ログは `validation/` に添付しています。

## 未実施・未確認

- 既存全資料の実データを使った結合ビルドと表示確認。
- ローカルHTTP経由のブラウザ遷移。実行環境のポリシーでlocalhostがブロックされたため、
  画面検証はオフラインDOMと模擬fetchに限定しました。
- 元画像の実表示、外部リンク・CDNの到達性、JavaScriptで動的に作る全参照先。
- GitHub Actionsの実実行、ブランチ保護との組合せ、Pagesへの実デプロイ。
- 利用者のPCにある `★仮置き保管庫` の場所・内容・OneDrive同期状態。

## 初期対応と保留対象

対応：HTML/HTM、PNG/JPEG/WebP/GIF、Markdown/TXT。
画像は原画像をそのまま利用し、テキストは全文をエスケープした閲覧HTMLにします。
サムネイルの新規描画・圧縮、Markdownのリッチ変換は含みません。
PDF/Office/ZIP/SVG/動画などは無理に変換せず保留します。
リンク欠落、UTF-8以外の本文、ローカル絶対参照、制御ファイル名の投入も安全に停止します。

## 参照した一次資料

実ファイル：
- https://github.com/TatsuyaOi/my-library/blob/6bef2fcefd88f5d702cde75bc1d32975cffbea5a/scripts/build_library.py
- https://github.com/TatsuyaOi/my-library/blob/6bef2fcefd88f5d702cde75bc1d32975cffbea5a/.github/workflows/rebuild-library.yml
- https://github.com/TatsuyaOi/my-library/blob/6bef2fcefd88f5d702cde75bc1d32975cffbea5a/AGENTS.md

公式仕様（2026-09-05確認）：
- https://developers.openai.com/codex/guides/agents-md
- https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site
- https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages
