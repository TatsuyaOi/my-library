# My Library Inbox対応版 導入手順

2026-09-05 / 既存カテゴリ・HTML・画像・公開URLを維持する更新です。

## 初回

1. この変更のPRでファイル差分と `rebuild` の検証結果を確認する。
2. GitHubリポジトリの Settings → Pages → Build and deployment → Source を **GitHub Actions** にする。
   すでにその設定なら変更不要。既存workflowを使うため、追加テンプレートは作らない。
3. PRを自分でマージする。Codexはマージしない。
4. Actions → Rebuild My Library indexes の `rebuild` と `deploy` の成功を確認する。
5. ローカルを利用する場合はGitHub Desktop等で最新mainを取得する。

Pages設定の現在値やデプロイ結果は、実際に確認するまで「設定済み／反映済み」と扱わない。
PR段階ではテストと公開対象の構築だけを行い、Pagesへは公開しない。

## 普段の使い方

`my-library/★仮置き保管庫` に原本を保存し、Codexへ「未処理資料を整理して。PRまで、マージしないで」と伝える。
必要な分類・閲覧HTML・meta.json・一覧更新・検証はルートAGENTS.mdの手順に従う。

Inboxは新規資料を受け付ける場所。カテゴリ一覧に追加する必要はない。
入力HTMLは原則そのまま、画像は原画像＋閲覧HTMLとして保存する。
Markdown/TXTの閲覧版は原文全文のテキスト表示であり、内容を要約しない。

処理元を残したまま新しい資料フォルダへコピーする。
同じ計画の再実行は重複作成しない。異なる内容での既存フォルダ上書きは拒否する。

## 公開前の注意

このリポジトリは公開用途。Inboxの.gitignoreは未追跡の原本の誤pushを防ぐためのもの。
追跡済みファイルには効かず、既にpushしたファイルの履歴を非公開にはできない。
Pagesに一覧表示されなくても、Public GitHubに置いた原本は公開される。
機密、個人情報、予約番号、APIキーを含む資料は保留し、公開しない。

ローカルCodexはPCの未追跡ファイルを扱える。
Codex Cloudで扱うには、公開可能な対象だけを作業ブランチへ明示的に追加する等、Cloudが実体を読める状態が必要。
原本を置くだけでCodexが常駐起動する機能は含めていない。

## 主要ファイル

| ファイル | 役割 |
|---|---|
| AGENTS.md | Codex向けの入口・分類・公開・検証ルール |
| docs/library-maintenance-rules.md | 旧AGENTS.mdの詳細ルールを原文保存 |
| docs/inbox-workflow.md | 使い方、計画JSON、対応形式、例外、Cloudの注意点 |
| inbox.config.json | ★仮置き保管庫の場所、容量上限、公開確認 |
| scripts/inbox.py | 原本一覧、dry-run、安全な新規取り込み |
| scripts/inbox_index.py | 新Inboxメタ情報と既存索引方式の接続 |
| scripts/build_library.py | 既存のカテゴリ別・全体索引生成 |
| scripts/prepare_pages.py | リンク・ファイル整合性検証、Inboxを除外した公開用出力 |
| assets/category-index.html | カテゴリ共通UIの生成元 |
| .github/workflows/rebuild-library.yml | PR検証、mainの索引生成・同一実行内でのPages公開 |

`library.config.json` のカテゴリはそのまま使用する。簿記は `22_簿記`、哲学は `11_哲学`、基本情報は `21_基本情報`。
`library-all.json`、カテゴリ `library.json`、カテゴリ `index.html` は生成物。手編集しない。
古いZIPや添付のlibrary-all.jsonで最新の一覧を上書きしない。

## 検証コマンド

リポジトリルートで、生成先資料フォルダを正確なパスでgit addしてから実行する。

```sh
python -m unittest discover -s tests -p 'test_*.py' -v
python scripts/build_library.py
python scripts/prepare_pages.py
```

Python 3.10以上。workflowは3.12、追加Pythonパッケージは不要。
既存のビルダーはGit追跡ファイルだけを索引化する。新規資料のステージ漏れに注意。

## トラブル対応

- 原本が見えない: 使用中のCodex環境とInboxの実在パス、Cloudならpush済みかを確認。
- 資料が索引に出ない: git add漏れ、hold、library-hidden、files.guideを確認。
- 画像が欠ける: 元HTMLが参照する全ファイルを、相対配置を崩さず同じ資料に含める。
- 既存保存先で停止: 内容が異なる資料を上書きしないための保護。新規IDまたは明示的な更新として扱う。
- Pages公開失敗: Source、workflow権限、github-pages環境のmain許可、Actionsのログを確認。
- 索引コミットが失敗: main保護や同時push競合を確認。force-pushしない。

`GITHUB_TOKEN`によるpushだけではPagesの別ビルドが起動しないため、
本workflowは生成済み `_site` を同じ実行からアップロード・デプロイする。

公式仕様:
https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site
https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages
