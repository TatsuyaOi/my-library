# My Library 自動化セット 導入手順

## できること

この構成では、普段の作業は次だけです。

1. ChatGPT / Codex などでHTMLを作る
2. 該当カテゴリのフォルダへHTMLを保存
3. GitHub Desktopで Commit
4. Push origin

Push後はGitHub Actionsが自動的に以下を行います。

- 各カテゴリ内のHTMLを探索
- HTMLのタイトル・説明・タグなどを取得
- 各カテゴリの `library.json` を生成
- 全カテゴリ横断の `library-all.json` を生成
- 共通テンプレートを各カテゴリの `index.html` に反映
- 生成物をmainブランチへ自動コミット
- GitHub Pagesが更新

---

## 初回導入

ZIPを展開し、中身を既存の `my-library` フォルダへコピーしてください。

既存の `index.html` やカテゴリ内の `index.html` は置き換えて構いません。

その後GitHub Desktopで、

- Summary: `My Library自動化を導入`
- Commit to main
- Push origin

を実行します。

GitHubのリポジトリ画面で `Actions` タブを開くと、
`Rebuild My Library indexes` が実行されます。

正常終了後、GitHub Actions自身が生成した
`library.json` / `library-all.json` をmainへコミットします。

---

## 普段のHTML追加

例：

`12_簿記/貸倒引当金.html`

を保存してCommit → Pushするだけです。

メタ情報がなくても、`<title>` やファイル名から自動登録されます。

より綺麗に表示したい場合はHTMLの `<head>` に以下を入れます。

```html
<meta name="library-title" content="貸倒引当金">
<meta name="library-group" content="決算">
<meta name="library-tags" content="簿記3級,仕訳,決算">
<meta name="library-description" content="貸倒引当金の意味・計算・仕訳を整理">
<meta name="library-pinned" content="false">
```

---

## 重要なファイル

### `assets/category-index.html`

全カテゴリ共通の画面デザインです。

ここを直してPushすると、GitHub Actionsが

- 01_哲学/index.html
- 11_基本情報/index.html
- 12_簿記/index.html
- ...

へ同じ内容を自動反映します。

つまりカテゴリごとのindexを個別編集する必要はありません。

### `library.config.json`

カテゴリ名・アイコン・説明・色を管理します。

カテゴリを増やす場合も、基本的にはここへ追加します。

### `scripts/build_library.py`

自動一覧生成の本体です。

### `.github/workflows/rebuild-library.yml`

Pushされたときに自動一覧生成を実行するGitHub Actions設定です。

### `library-all.json`

全カテゴリ横断検索と「最近更新した資料」に使う自動生成ファイルです。

---

## 新しいカテゴリを追加する場合

`library.config.json` の `categories` に追加します。

例：

```json
"51_読書": {
  "title": "読書",
  "icon": "📚",
  "description": "読書ノート・本の要約",
  "accent": "#7a5b3a"
}
```

Pushすると、

`51_読書/index.html`
`51_読書/library.json`

がGitHub Actionsによって自動生成されます。

---

## GitHub Actionsが失敗する場合

GitHubの

Settings → Actions → General

周辺でWorkflowの権限設定が制限されている場合があります。

このセットではworkflow内で

`permissions: contents: write`

を指定していますが、組織やリポジトリ設定で書き込みが禁止されている場合は
自動コミットに失敗します。

またmainブランチに強いBranch protectionを設定している場合も
直接pushできないことがあります。

個人用の通常のPublic repositoryなら、そのまま動く構成です。
